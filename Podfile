# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SchoolsApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SchoolsApp
pod 'Alamofire', '~> 4.7'
pod 'GooglePlaces'
pod 'CSV.swift', '~> 2.1.0'

  target 'SchoolsAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SchoolsAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
